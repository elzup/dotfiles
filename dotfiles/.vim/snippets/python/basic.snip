snippet scrape_start
abbr スクレイピング
options head
    from urllib.request import urlopen
    from bs4 import BeautifulSoup
    
    url = "${0}"
    page = urlopen(url)
    bs = BeautifulSoup(page)
    for li in (bs.find('li')):
        print(li.string)

snippet time_measure
abbr 実行時間計測
options head
    import time
    start = time.time()
    
    print(str(time.time() - start) + "ms")
